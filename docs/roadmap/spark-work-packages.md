# Spark support: implementation work packages (v2, review applied)

Generated 2026-09-02 from spec v2. The fixes from spark-spec-review.md are folded into spark-support.md v2 and spark-rules.json (status `v2-review-applied`); implement against those files, not the review. Cross-cutting v2 additions: `Finding.Impact` (WP1), `impact` / `Advisory:` rendering and the closed `platforms` set (WP2), `on_package` assertions in the gb10 sim (WP4), impact/advisory documentation (WP5).

**Status (2026-09-02):** WP1 (branches `wp/1a` collectors/common and `wp/1b` Linux/Windows collectors), WP2 (`wp/2`), WP3 (`wp/3` plus `fix/llmplan`), WP4 (`wp/4`) and WP5 (`wp/5`) are implemented, reviewed and merged to `main`. Because the packages were written in isolation, the seams are integrated on the `int/*` branches: runner wiring of the WP1b collectors and the `DGXOSInfo.UnitsQueried` contract (collectors / analyzer), and `int/3` (this document, CI simulation, shims, scripts and docs alignment). Per-package status lines are below each heading; nothing has run on GB10 or N1X hardware yet (issue #3, `scripts/spark-capture.sh`).

## WP1: Types and collectors: platform, unified memory, DGX OS, ConnectX-7, ecosystem, Windows on Arm (+ self-test, redaction, snapshot)

Status: done; merged to `main` 2026-09-02 from `wp/1a` (`common/platform.go`, `unified_memory.go`, `simroot.go`, gpu/thermal/pcie/system changes, selftest, redact, snapshot, the phase-1 / phase-4 runner hooks) and `wp/1b` (`linux/dgxos.go`, `dgxos_host.go`, `cx7.go`, `nvrm.go`, `ai/ecosystem.go`, `windows/woa.go`, `gpu_woa.go`; types `WoAInfo`, `PlatformInfo.UncleanBoots`, `LinuxInfo.GSPFailureLines`). Integration: `DGXOSInfo.UnitsQueried` marks whether `systemctl` answered (landed: `dgxos.go` sets it, the analyzer gates the dashboard rule on it). Pending on the collector integration branch: wiring `linux.CollectDGXOS` / `CollectDGXHostState`, `CollectCluster`, `CollectNVRMMessages`, `ai.CollectEcosystem` and `windows.CollectWoA` into `internal/core/runner_platform.go` (still the phase-4 stub on `main`) or its tagged siblings, and redacting only the address part of the ConnectX-7 CIDR entries in `redactCluster`; until then `cluster` / `ecosystem` are absent from real and simulated reports and the `gb10` CI assertions on them stay red.

**Ownership:** pkg/types/types.go; internal/collector/common/{platform.go,unified_memory.go,gpu.go,nvsmi.go,thermal.go,pcie.go,system.go}; internal/collector/linux/{dgxos.go,cx7.go,ecosystem.go,xid.go}; internal/collector/windows/{woa.go,gpu_woa.go}; internal/collector/ai/*; internal/collector/wsl/wsl.go; internal/selftest/*; internal/redact/*; internal/snapshot/*; internal/core/runner*.go (phase wiring only).

Spec sections 3-4. (1) Add PlatformInfo (field `NvidiaKernelFlavour`, not `DGXKernel`), DGXOSInfo, UnifiedMemoryInfo, FirmwareComponent, FabricPort/ClusterInfo, EcosystemInfo and the GPUInfo/ThermalInfo/PCIeInfo/PyTorchInfo additions exactly as in section 4, plus `Impact string json:"impact,omitempty"` on `pkg/types.Finding` (values none | reversible | persistent | irreversible | data-loss; the rule schema in knowledge/rules.json gains the same `impact` key) (SchemaVersion stays "1"). (2) common/platform.go: DetectPlatform() (phase 1, files/lspci/DMI/kernel rows only) and ApplyPlatformFlags(report) (called from internal/core/runner.go after phase 3, before phase 4) per table 3.1 with the PCI-ID rows (10de:2e12, then 2e03/2e06) evaluated before any compute-capability heuristic (the CC 12.1 + [N/A] fallback no longer asserts a class); ApplyPlatformFlags evaluates rows 5/7/8/9 from report.GPUs and then applies flag rules A-C of 3.1 to every GPUInfo/PCIeInfo: class-derived UnifiedMemory/OnPackage for dgx-spark and rtx-spark whatever row matched, and the platform-independent rule that any NVIDIA GPU whose memory.total is [N/A] sets GPUInfo.MemoryReporting=not-supported, GPUInfo.OnPackage, PCIeInfo.OnPackage and PlatformInfo.UnifiedMemory even when Class is empty; set NvidiaKernelFlavour with the 3.1 regex (-64k / -lowlatency accepted); parse /etc/dgx-release, /etc/fastos-release, /sys/class/dmi/id/*, lscpu Model name lines with MIDR fallback; honour NVC_SIM_ROOT for /etc, /proc/meminfo, /proc/cpuinfo and /sys paths. (3) gpu.go/nvsmi.go: tolerant GPUCapQueryFields="index,compute_cap"; isNotAvailable accepts bare 'Not Supported'; keep the full BDF (domain 000f) in PCIBusID; GB10 explanation for 'No devices were found' when lspci shows 10de:2e12. (4) unified_memory.go: /proc/meminfo, /proc/swaps + zram, vm.swappiness, /proc/pressure/memory, /proc/vmstat pswpin per sample (for the swap-in-use rule), AllocatableKB per 3.3; only when UnifiedMemory. (5) thermal.go: on GB10 parse nvidia-smi -q -d PERFORMANCE 'Clocks Event Reasons Counters' into EventCounters. (6) linux/dgxos.go: dpkg pairing (driver vs firmware vs modules-for-kernel), nvidia-spark-ota-check summary/torn-score (10 s timeout), systemd states (dgx-dashboard, dgx-dashboard-admin, fwupd, nvidia-persistenced, gb10-clock-cap), 127.0.0.1:11000 probe, fwupdmgr get-devices (parse dotted and hex version forms), nvidia-container-toolkit.list first line, journalctl --list-boots and -b -1 tail classification (clean markers: 'Journal stopped', 'systemd-shutdown', 'Shutting down.', 'Reached target Power-Off/Reboot/Halt'; count unclean boots over N days), /sys/fs/pstore, acpitz zones, GDM sleep policy, suspend markers; read-only, skipped off GB10. (7) linux/cx7.go: 15b3 functions, /sys/class/infiniband ports + device/net mapping, netdev operstate/speed/mtu/IPv4, cage grouping by function index across domains 0000/0002, bonds, /etc/nvidia/cx7-hotplug-enabled, netplan keys, NCCL_*/UCX env of this process, avahi state/conflicts, /etc/ufw/ufw.conf, rdma tools; no active probes. (8) linux/ecosystem.go + ai: torch probe stderr and arch list, triton ptxas version/TRITON_PTXAS_PATH, libcudart.so.12/.13 presence, flash_attn/onnxruntime distributions and ORT providers, docker image architectures/tags, daemon.json runtimes/CDI, /etc/cdi/nvidia.yaml, snap docker, listening inference ports (8000, 30000, 11434, 8355, 11000, 7474) without process names. (9) windows/woa.go: IsWow64Process2 via syscall.NewLazyDLL("kernel32.dll"), PROCESSOR_ARCHITECTURE/ARCHITEW6432, Win32_Processor.Architecture, Win32_ComputerSystemProduct, PNP DEV_2E03/2E06, INF, dxdiag dedicated/shared memory, nvcc.exe PE machine type, build number; missing nvidia-smi on rtx-spark is INFO. (10) xid.go: add 120 'GSP task exception'. (11) selftest: GPUCapQueryFields; on GB10 report memory [N/A] as expected. (12) redact: DGX_SERIAL_NUMBER, DMI serials, fabric LAN IPs, spark-xxxx hostnames. (13) snapshot Diff: platform.class, ota_version, mem_total_kb, firmware versions. Fixture tests for every parser using the verbatim strings of section 3.

## WP2: Analyzer rules, suppressions, knowledge pack, report rendering

Status: done; merged to `main` 2026-09-02 from `wp/2` (`analyzer_spark.go`, `analyzer_cluster.go`, `analyzer_woa.go`, `analyzer_spark_rules.go`, fixtures, 51 rules in `knowledge/rules.json` with `impact` on every rule, `report/spark.go`, Platform block, Advisory rendering, golden files). Integration: the dashboard rule is gated on `dgx_os.units_queried` (integration branch, analyzer side).

**Ownership:** internal/analyzer/** (analyzer.go, analyzer_spark.go, analyzer_cluster.go, analyzer_woa.go, tests); knowledge/rules.json; knowledge/xid_codes.json; internal/report/{text.go,markdown.go,json.go}.

Spec sections 5-6. (1) analyzerSourceIDs scans every non-test .go file in the package. (2) Implement the 51 rules with the exact ids, severities, categories, confidences, impact values and next steps (`Advisory:` prefixes and revert commands verbatim; trigger variants as documented); one ruleCorpus report per rule; add each to knowledge/rules.json with modes, platforms and impact; add a knowledge test that every platforms[] entry is in {dgx-spark, rtx-spark, jetson, grace-hopper, arm64-dgpu, all} and every impact is one of the five values. (3) Suppressions when Platform.UnifiedMemory: skip analyzeVRAM; skip pcieFindings for OnPackage GPUs (PCIeInfo.OnPackage comes from the 3.1 flag rules A/B, so stock DGX OS is covered; the gb10 fixture with 'GEN 1@ 1x' asserts pcie-width-reduced absent); test that fan [N/A] never yields fan-not-spinning; power-cap/slowdown evidence prints 'limit N/A (unified memory)'; rtx-spark without nvidia-smi.exe downgrades nvidia-smi-missing/driver-not-detected to INFO; nvidia-app-detected not expected on ARM64; jetson-detected wording covers Thor. (4) 'No devices were found' + lspci 10de:2e12 -> dgx-spark-gsp-init-failure instead of no-nvidia-gpu. (5) Summary block 'Unified memory: X GiB total, Y GiB available' replaces 'VRAM: N MB' plus a 'Platform:' line; PCIe line 'n/a (on-package, NVLink-C2C)'; new UNIFIED MEMORY, DGX OS, FIRMWARE, CLUSTER FABRIC sections; report.json gains platform, unified_memory, cluster, ecosystem. (5b) internal/report/text.go and markdown.go: print each finding's impact next to its severity and render next steps that begin with the word Advisory (regex ^Advisory followed by a word boundary) distinctly (own marker/indent, the revert command kept on the same line, never listed before a read-only step); json.go emits Finding.Impact through the struct tag (omitempty keeps existing reports unchanged). (6) xid_codes.json: 119 pairing context, 120 GSP task exception. (7) Tests: the healthy GB10 report yields exactly the expected INFO set and none of the forbidden ids of the scenario (incl. no dgx-spark-foreign-driver-packages while nvidia-dkms-580-open is installed); unified-memory-swap-in-use INFO vs WARN split on MemAvailable and no swapoff advice without the stop-the-workload wording; gb10-logless-hard-poweroff silent for a boot ending in "Journal stopped" and needing >= 2 unclean boots; gb10-pd-power-wedge WARN on one matching sample and CRIT only when every sample matches; every emitted finding has a non-empty Impact.

## WP3: LLM optimization wizard: nvcheckup llm-plan

Status: done; merged to `main` 2026-09-02 from `wp/3` (`cmd/nvcheckup/llmplan.go`, `internal/llmplan`, `knowledge/models.json`, doctor question 7) and `fix/llmplan` (WDDM driver strings on RTX Spark, multi-GPU hosts sized against the largest GPU). `examples/sample-llm-plan.txt` is the wizard's own `plan.txt` for the GB10 fixture report. CLI as shipped: `--list-models`; `--model llama-3.1-8b-instruct --profile agent --runtime vllm --json --out DIR`; `--model llama-3.3-70b-instruct --quant nvfp4 --context 131072`; `--report report.json` (offline); exit 0 / 1 / 2 / 3; `plan.json` `fit.total_gib` 51.0 and `fit.gpu_memory_utilization` 0.40 for the 8B agent example, which the simulated `gb10` job asserts.

**Ownership:** cmd/nvcheckup/main.go (dispatch + usage lines only); cmd/nvcheckup/llmplan.go; internal/llmplan/** (sizing.go, models.go, runtimes.go, prereqs.go, render.go, tests); knowledge/models.json; internal/doctor/doctor.go (one new Spark question).

Spec section 7. (1) CLI flags of 7.1 with interactive prompts when --model is absent. (2) knowledge/models.json with the 7.3 shapes and sources; --hf-config parses a local config.json offline. (3) sizing.go implements 7.4 exactly, including both decode ceilings (weights-only and at-context); unit tests reproduce the three worked examples of 7.5 to +/-0.1 GiB and the ceilings 17 / 13.4 tok/s (8B BF16, 32K) and 6.9 / 3.3 tok/s (70B NVFP4, 128K) to +/-0.1; the 64 GB tier is never read from a table (measured TotalVisibleMemorySize / MemTotal only). (4) runtimes.go renders the 7.6 templates; never emits vLLM --kv-cache-dtype fp8 on sm_121 unless --kv-dtype fp8 is given. (5) prereqs.go evaluates the 7.7 checklist from existing read-only collectors only. (6) render.go: text plan and plan.json (7.8); exit 0 fits / 1 fits with warnings / 2 does not fit / 3 error. (7) Enforce 7.9. (8) doctor: on GB10/N1X ask 'Plan an LLM deployment for this machine?' and hand off.

## WP4: windows/arm64 target, CI runners, GB10 simulation, field kit

Status: done; merged to `main` 2026-09-02 from `wp/4` (ci.yml, release.yml, linux-fieldtest-sim.yml `gb10` job, shims, `gb10.json` / `gb10-gsp-fail.json`, `scripts/make-simroot.sh` with committed simroot trees, `scripts/spark-capture.sh`). Integration on `int/3`: `journalctl` and `ldconfig` shims, `systemctl show -p LoadState`, netplan / hotplug / Docker / CDI / apt fixtures in the simroot, assertions for `dgx_os.units_queried`, the ConnectX-7 ports, the ecosystem section and redaction, the real `llm-plan --report` step, and the remaining unconfirmed captures in `spark-capture.sh`.

**Ownership:** .github/workflows/*; .github/fieldtest/shims/* (incl. new dmidecode, lscpu, ibdev2netdev, fwupdmgr, nvidia-spark-ota-check, dpkg shims); .github/fieldtest/scenarios/{rig3.json,gb10.json,gb10-gsp-fail.json}; scripts/linux-fieldtest.sh; scripts/spark-capture.sh.

Spec sections 8-10. (1) release.yml: add windows/arm64 (nvcheckup-windows-arm64.exe) to the build matrix, attestation subjects and download table; ci.yml: runs-on windows-11-arm plus GOOS=windows GOARCH=arm64 vet. (2) gb10.json (section 10) and shim extensions: nvidia-smi shim adds compute_cap, prints [N/A] verbatim, renders 'Not Supported' Memory-Usage and N/A fan/cap in the table, supports -q -d PERFORMANCE; lspci/dpkg/lsmod/dmesg shims read *_lines; the job writes /etc/dgx-release, /etc/fastos-release and DMI/meminfo/cpuinfo/thermal fixtures under NVC_SIM_ROOT. (3) Sim jobs on ubuntu-24.04-arm: 'gb10' asserts exit 0|1, expected findings present, forbidden absent, summary has 'Unified memory' and 'Platform: DGX Spark', platform.class == dgx-spark, platform.unified_memory == true, pcie.on_package == true, gpus[0].on_package == true, gpus[0].memory_reporting == not-supported, unified_memory.mem_total_kb == 125513944, every finding carries impact; 'gb10-gsp-fail' (nvidia-smi 'No devices were found', dmesg SEC2/GSP lines) asserts dgx-spark-gsp-init-failure CRIT and no no-nvidia-gpu. (4) linux-fieldtest.sh / spark-capture.sh capture the section 12 fixture set, redacted. (5) Publish examples/sample-report-dgx-spark.txt as a sim artifact for WP5.

## WP5: Documentation

Status: done; merged to `main` 2026-09-02 from `wp/5` (README Spark section and `llm-plan` reference, PRODUCT, CHANGELOG Unreleased, CONTRIBUTING, SECURITY, `docs/index.html`, `docs/assets/unified-memory.svg`, `examples/sample-report-dgx-spark.txt`, `examples/sample-llm-plan.txt`, spec banner and section 11). Integration on `int/3`: the docs were aligned with the code that landed (real `llm-plan` flags and model ids, `units_queried`, the `/proc/net/tcp` dashboard check, shim and fixture lists, regeneration commands).

**Ownership:** docs/roadmap/spark-support.md (replaced by this spec); README.md; PRODUCT.md; CHANGELOG.md; CONTRIBUTING.md; docs/index.html; examples/sample-report-dgx-spark.txt; description text in knowledge/*.json (ids owned by WP2).

Spec section 11. (1) Replace docs/roadmap/spark-support.md with this spec; keep section 12 current. (2) README: platform rows for DGX Spark (FE + OEM), RTX Spark (windows/arm64), Jetson Thor note; Supported GPUs row for GB10/N1X (no VRAM figure, PCIe n/a, fan and power limit N/A by design); Command Reference for llm-plan with the three worked examples; Privacy additions (what is newly read; never changing clocks or swap; explain the impact field and that 'Advisory:' steps are printed advice with a revert command, not actions NVCheckup takes); FAQ entries for 'Not Supported' memory and the missing PCIe line. (3) PRODUCT.md: platform table (windows/arm64), Data Collected sections, rule categories with the new ids, report.json additions incl. finding.impact (schema_version stays 1), llm-plan, Does-Not-Do additions. (4) CHANGELOG Unreleased entries. (5) CONTRIBUTING: GB10/N1X fixture wish-list (section 12 commands); rule ids may live in analyzer_*.go. (6) docs/index.html platform pill. (7) Commit examples/sample-report-dgx-spark.txt from the WP4 artifact.

## Open questions (need real hardware)

- Verbatim nvidia-smi -L line on GB10 and the CSV value of every queried field: pstate and clocks.current.graphics at idle, clocks.max.graphics via --query-gpu (3003 seen only in -q), pcie.link.gen.max/width.max (Gen1 x1 misreport or [N/A]), clocks_event_reasons.active at idle and under load, compute_cap, power.draw formatting.
- Whether /proc/device-tree/model exists on DGX Spark (ACPI-only boot suspected) and its contents on FE and OEM units; origin of 'NVIDIA NVIDIA_DGX_Spark NVIDIA Tegra NVIDIA GB10' seen in an SGLang issue title.
- Whether OEM GB10 systems change /etc/dgx-release fields or the MOTD and ship nvidia-spark-ota-check, dgx-dashboard and dgx-spark-fieldiag; exact OEM DMI strings beyond HP 'HP ZGX Nano G1n AI Station', Lenovo '30KL0004FC'/'ThinkStation PGX', ASUS BIOS 'GX10DGX.0102.2025.1111.1531'.
- Exact key of the FASTOS build line some users see in /etc/dgx-release; mapping of OTAyymm names (only OTA2607 = July 2026 = 580.159.03 confirmed) to DGX_OTA_VERSION; which OTA pairs 580.173.02.
- Verbatim fwupdmgr get-devices names and version formats on FE and OEM units; confirmation of 0x03000508 = 3.5.8, 0x02009b0b = 2.155.11, 0x00000516 = 0.5.22.
- Default swap on DGX OS 7 (swapfile vs zram, size; ~15-16 GiB reported) and changes across OTAs; MemTotal 119.7 GiB (2025) vs 121.7 GiB (2026) and its relation to the 2 GB / 4 GB Display Reserved Memory toggle.
- Whether nvmlDeviceGetMemoryInfo on 590/595 returns MemTotal-like values instead of NVML_ERROR_NOT_SUPPORTED; verbatim nvidia-smi -q -d UTILIZATION,PERFORMANCE,CLOCK output at idle and under load, including 'Clocks Event Reasons Counters'.
- The clocks_event_reasons.active mask in the USB-PD wedge state (0x0 expected) and whether a growing 'SW Power Capping' counter with 'SW Power Cap: Not Active' is a reliable discriminator; whether the PD contract is readable from /sys/class/typec or ucsi.
- Whether July 2026 firmware (EC 0x03000508, PD 0x00000516) fixes the PD wedge and the log-less hard power-off; whether NVIDIA endorses any clock cap; the EC's hard-cut thresholds; whether the EC fan-curve regression exists on FE or only OEM GX10 builds; which /sys/class/thermal zone maps to GPU vs SoC.
- Kernel page size on DGX Spark (4K vs 64K); whether nvidia-persistenced is enabled by default; cudaDeviceProp values on GB10 (integrated, pageableMemoryAccess, concurrentManagedAccess, directManagedMemAccessFromHost); the exact cudaMemGetInfo total (122,570 vs 124,618 MiB); what the 580.159.03 'user feedback under memory pressure' looks like.
- Exact ibstat 'Link layer' and /sys/class/infiniband/roce*/ports/1/rate strings; known-good CX-7 firmware after the Feb 2026 fix; whether DGX OS ships ufw enabled; ports required by NVIDIA Sync Cluster Assistant; default netplan/NM handling of the twins.
- RTX Spark (N1X): compute capability (12.1 inferred), whether nvidia-smi.exe ships in the 616.00 Arm64 package and what it prints for memory, Win32_Processor.Name / SMBIOS strings, DXGI dedicated vs shared defaults and the 'Reserved memory for accelerators' carve-out, memory tiers per OEM, WDDM version, bandwidth (273 vs ~300 GB/s), 45-80 W thermal behaviour, and availability of an Arm64 NVIDIA app, Windows ML NvTensorRtRtx EP and WSL2 CUDA at launch.
- Whether NVIDIA officially supports DCGM/dcgm-exporter and GPU Operator on GB10.
- Whether a stock DGX OS install carries nvidia-dkms-580-open (dpkg -l 'nvidia-dkms*'); dgx-spark-foreign-driver-packages excludes it until confirmed, and the gb10 fixture's dkms line is a placeholder.
- Whether the RTX Spark WDDM DriverVersion is really 32.0.16.1600 (only the trailing 16.1600 is matched) and whether WSL2 GPU passthrough exists on RTX Spark devices other than the Surface Dev Box (S122).
- Bytes per parameter for GGUF Q4_K_M/UD-Q4_K_XL from a primary source; per-slot state cost of hybrid Mamba models (Nemotron 3 Super ~7 GB per Ollama slot at 262K); how vLLM/llama.cpp allocate KV for gpt-oss sliding-window layers; vLLM runtime/JIT overhead range on GB10 (3+13 GB vs 50 GB transient).
