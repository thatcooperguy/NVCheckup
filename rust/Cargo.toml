[package]
name = "nvcheckup"
version = "0.2.0"
edition = "2021"
description = "Cross-platform NVIDIA diagnostic tool — Rust companion"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
